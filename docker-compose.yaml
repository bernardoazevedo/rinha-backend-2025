services:
  nginx:
    image: nginx:1.25-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    ports:
      - "9999:9999"
    networks:
      - api
      - payment-processor
    # deploy:
    #   resources:
    #     limits:
    #       cpus: "0.15"
    #       memory: "15MB"

  api1:
    build: .
    volumes:
      - ./:/app
    # depends_on:
    #   - redis
    networks:
      - api
      - payment-processor
    # deploy:
    #   resources:
    #     limits:
    #       cpus: "0.5"
    #       memory: "150MB"

  api2:
    build: .
    volumes:
      - ./:/app
    # depends_on:
    #   - redis
    networks:
      - api
      - payment-processor
    # deploy:
    #   resources:
    #     limits:
    #       cpus: "0.5"
    #       memory: "150MB"
          
  # redis:
  #   image: "redis:alpine"
  #   ports:
  #     - 6379:6379
  #   volumes:
  #     - redis-data:/data
  #   networks:
  #     - api
    # deploy:
    #   resources:
    #     limits:
    #       cpus: "0.35"
    #       memory: "35MB"
# volumes:
#   redis-data:

networks:
  api:
    driver: bridge
  payment-processor:
    external: true